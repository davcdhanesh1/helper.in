<header>
  <hr>
  <div class="navbar" id="header">
    <div class="container">
      
      <div class="row">
        <div class="col-sm-2 center">
          <a href=<%= root_path %> class="btn btn-lg btn-primary"><b>Helper</b></a>
        </div>
        <div class="col-sm-6"></div>
        <div class="col-sm-4">
          <ul class="nav navbar-nav center">
            <% if logged_in? %>
              <li><%= link_to("New worker", new_worker_url) %> </li>
              <li><%= link_to("Logout", logout_url, method: "delete") %><li>
            <% else %>
              <li>
              <a href="#">
                <span>How does it work</span>&nbsp&nbsp<span class="glyphicon glyphicon-info-sign"></span>
              </a>
              </li>
              <li>
                <a href="#">
                  <span>Call us for anything</span>&nbsp&nbsp<span class="glyphicon glyphicon-phone-alt"></span>
                </a>
              </li>
            <% end %>
          </ul>
        </div>
      </div>

    </div>
  </div>
  <hr>
</header>
